# CEO onboarding kit (vendored)

These four files are copied from [paperclipai/companies `default/ceo`](https://github.com/paperclipai/companies/tree/main/default/ceo) so Hypowork onboarding does not depend on GitHub at runtime.

They are served as static assets from the web app at `/onboarding/ceo/*.md`.
