import { Check, Copy, Download } from 'lucide-react';

/** Minimal icon set for shared plate / codeblock UI (lucide-backed). */
export const Icons = {
  check: Check,
  copy: Copy,
  download: Download,
};
