# Previous fixes (institutional memory)

Short write-ups of bugs we already solved so agents and humans do not re-debug from zero.

**Process:** See [AGENTS.md](../AGENTS.md) — check this index **before** debugging; add a doc and table row **after** each successful fix.

| Topic | Doc |
| ----- | --- |
| Migrations stuck or failing on `CREATE EXTENSION vector` with embedded Postgres | [embedded-postgres-pgvector-migrations.md](./embedded-postgres-pgvector-migrations.md) |

When you fix a recurring class of issue, add a row and a focused markdown file here. Link env vars and migration filenames so searches hit.
