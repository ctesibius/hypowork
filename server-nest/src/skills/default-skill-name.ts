/**
 * Tier-1 default skill (`server/skills/hypowork-default.md`), forked into `prompt_versions` per company.
 * Used for instance chat / editor attribution (`promptVersionId`).
 */
export const INSTANCE_DEFAULT_SKILL_NAME = "hypowork-default" as const;
